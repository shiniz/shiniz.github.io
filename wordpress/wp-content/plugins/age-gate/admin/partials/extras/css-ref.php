<h3>.age-restriction</h3>
<p><?php esc_attr_e( 'Added to the <code>body</code> element.', $this->plugin_name); ?></p>

<h3>.age-gate-wrapper</h3>
<p><?php esc_attr_e( 'A wrapper <code>div</code> containting all age gate content', $this->plugin_name); ?></p>

<h3>.age-gateage-gate-form</h3>
<p><?php esc_attr_e( 'Class added to the <code>form</code> element', $this->plugin_name); ?></p>

<h3>.age-gate-heading</h3>
<p><?php esc_attr_e( 'The <code>H1</code> element containing the site title', $this->plugin_name); ?></p>

<h3>.age-gate-heading.age-gate-logo</h3>
<p><?php esc_attr_e( 'The <code>H1</code> element if you have added a logo', $this->plugin_name); ?></p>

<h3>.age-gate-subheading</h3>
<p><?php esc_attr_e( 'Secondary heading - <i>&ldquo;Please enter your date of birth&rdquo;</i> by default', $this->plugin_name); ?></p>

<h3>.age-gate-message</h3>
<p><?php esc_attr_e( 'THe message set before the age inputs', $this->plugin_name); ?></p>

<h3>.age-gate-form-elements</h3>
<p><?php esc_attr_e( 'A <code>UL</code> containing the form fields', $this->plugin_name); ?></p>

<h3>.age-gate-form-section</h3>
<p><?php esc_attr_e( '<code>LI</code> elements containing the <code>label</code> and <code>input</code> or <code>select</code> fields', $this->plugin_name); ?></p>

<h3>.age-gate-label</h3>
<p><?php esc_attr_e( 'The <code>label</code> for each field', $this->plugin_name); ?></p>

<h3>.age-gate-input</h3>
<p><?php esc_attr_e( 'Added to the <code>select</code> or <code>input</code> fields', $this->plugin_name); ?></p>

<h3>.age-gate-remember-wrapper</h3>
<p><?php esc_attr_e( 'Element wrapping the remember me option', $this->plugin_name); ?></p>

<h3>.age-gate-submit</h3>
<p><?php esc_attr_e( 'The default submit button', $this->plugin_name); ?></p>

<h3>.age-gate-submit-yes</h3>
<p><?php esc_attr_e( 'The &ldquo;Yes&rdquo; button when using Yes/No option.', $this->plugin_name); ?></p>

<h3>.age-gate-submit-no</h3>
<p><?php esc_attr_e( 'The &ldquo;No&rdquo; button when using Yes/No option.', $this->plugin_name); ?></p>

<h3>.age-gate-additional-information</h3>
<p><?php esc_attr_e( 'Wrapper element for the "Additional Content" text', $this->plugin_name); ?></p>
